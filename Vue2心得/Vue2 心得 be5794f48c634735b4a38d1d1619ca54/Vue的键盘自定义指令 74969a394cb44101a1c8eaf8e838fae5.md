# Vue的键盘自定义指令

# Vue的键盘自定义指令

**Vue**.dirctive(**'on'**).keyCode.**ctrl**=17;

@click.ctrl="test(ev)"

--通过js来判断按键的键码

**@keydown="show";**

**show:function** **(event) {**

***console*.log(event.keyCode);**

**};**