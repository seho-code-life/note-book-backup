# 102

# 102

行内元素、块级元素、空(void)元素分别有哪些？

行内元素： a, span, img, input, b,strong,select,button

块级元素：div,p,article,seletion,video,ul,ol,h1-h6,table,form

空标签： img,br,hr,input

你是如何规划响应式布局的？

pc一般是百分比搭配flex

mobile一般是flex和rem

准确说出'1,2,3,4'.split()的结果是什么（包括类型和值）？

答案：["1,2,3,4"] // 长度1，类型是Array<String>

split接受2个参数

1. 可以传递字符串或者正则，以什么元素进行转换数组，如果不传就返回以当前字符串为值且长度为一的数组（如题）

如果传递了""，那么返回的数组则是字符串全部分割返回的数组

2. limit限制了转换数组返回的长度，如果超过了limit，那么返回的数组将会截断

进程与线程有什么区别？JS的单线程带来哪些好处？

一个程序必定包含 》= 一个进程

一个进程必定包含 》= 一个线程

进程之间不共享内存，每新开一个进程都要开辟新的内存

线程之间共享内存，因此多线程可以提高程序的并发性

js最早是面向浏览器的脚本语言，开发者在对dom做处理时，如果是多个线程，引擎就不知道时哪个线程做了操作，这样的程序会很紊乱

因此js是要和用户实时交互的，所以是单线程

在一些复杂业务中，我们也可以使用web-woker来做计算处理，但是它不能操作dom